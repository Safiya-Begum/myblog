import { LimitVisibleContentPipe } from './limit-visible-content.pipe';

describe('LimitVisibleContentPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitVisibleContentPipe();
    expect(pipe).toBeTruthy();
  });
});
